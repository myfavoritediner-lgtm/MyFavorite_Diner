# Store everything with LF in the repository, check out native on Windows.
# Without this, every file looks modified when the project moves between machines.
* text=auto eol=lf

# Binary assets — never touch these.
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.webp binary
*.ico  binary
*.pdf  binary
